# Optional: if unset, the frontend auto-detects the backend host from the
# page's own URL (localhost or LAN IP), on port 3000. Set this only if the
# backend runs on a different host than the frontend.
# VITE_API_URL=http://localhost:3000/api
